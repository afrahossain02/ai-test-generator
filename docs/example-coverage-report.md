# Test coverage report

_Posts and users endpoints of the JSONPlaceholder API._

| | |
|---|---|
| Source | OpenAPI specification |
| Generated | 2026-09-06T12:00:00.000Z |
| Test cases | 10 |
| Automated | 9 of 10 (90%) |
| Steps translated | 10 of 10 (100%) |
| Assertions | 21 |
| Last run | 9 passed, 0 failed, 1 skipped |

## Gaps to review

- **Low** — Only one boundary case, against 4 elsewhere.
  Uneven depth usually means a category was filled to satisfy the format rather than the risk.
- **Low** — Only one security case, against 4 elsewhere.
  Uneven depth usually means a category was filled to satisfy the format rather than the risk.

## Coverage by category

| Category | Cases | Automated |
|---|---|---|
| Happy path | 4 | 4 |
| Edge cases | 2 | 1 |
| Negative | 2 | 2 |
| Boundary | 1 | 1 |
| Security | 1 | 1 |

## Coverage by priority

| Priority | Cases |
|---|---|
| P0 | 5 |
| P1 | 3 |
| P2 | 2 |

## Cases

### Happy path

#### TC-001 · Listing posts returns a non-empty collection

`P0` · `automated` · `last run: passed`

**Why it matters:** The catalogue endpoint returning 200 with an empty array looks healthy to a monitor but is a total outage to a caller.

#### TC-002 · Fetching a known post returns that post

`P0` · `automated` · `last run: passed`

**Why it matters:** Catches an off-by-one in id lookup that returns a neighbouring record — the status code stays 200 while the data is wrong.

#### TC-003 · Fetching the comments on a post returns a collection

`P1` · `automated` · `last run: passed`

**Why it matters:** Catches a broken join on the nested route, which typically returns an empty array rather than an error.

#### TC-004 · Creating a post echoes the submitted fields with a new id

`P0` · `automated` · `last run: passed`

**Why it matters:** Catches a create that returns 201 while silently dropping or mangling the submitted fields.

### Edge cases

#### TC-008 · Filtering posts by author returns a usable collection

`P2` · `automated` · `last run: passed`

**Why it matters:** Catches an unrecognised query parameter being ignored — the endpoint returns 200 with every post, which looks correct until you count them.

#### TC-010 · A single post is served well within the latency budget

`P2` · `needs work` · `last run: skipped`

**Why it matters:** Catches a missing index on the posts table, which shows up as latency long before it shows up as an error.

Blocking automation:

- final assertion: The response is returned in under 500 milliseconds.

### Negative

#### TC-005 · An unknown post id is refused rather than guessed at

`P0` · `automated` · `last run: passed`

**Why it matters:** Catches an unknown id falling through to a 200 with an empty body, which a client cannot distinguish from a real record.

#### TC-007 · An unknown user id is refused

`P1` · `automated` · `last run: passed`

**Why it matters:** Confirms the not-found path is wired on the users route too, not just on posts.

### Boundary

#### TC-006 · Post id zero is treated as not found, not as a falsy default

`P1` · `automated` · `last run: passed`

**Why it matters:** Zero is the classic falsy-id bug: a truthiness check on the id treats 0 as absent and can return the whole collection or the first record instead of a 404.

### Security

#### TC-009 · A user record does not leak credential material

`P0` · `automated` · `last run: passed`

**Why it matters:** Catches a serializer that returns the whole user row, exposing a password hash to any caller who can read a profile.

---

Generated by [ai-test-generator](https://github.com/afrahossain02/ai-test-generator).
